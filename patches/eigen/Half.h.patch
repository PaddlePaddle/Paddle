--- a/Eigen/src/Core/arch/Default/Half.h
+++ b/Eigen/src/Core/arch/Default/Half.h
@@ -773,7 +773,8 @@ struct random_default_impl<half, false, false>
 {
   static inline half run(const half& x, const half& y)
   {
-    return x + (y-x) * half(float(std::rand()) / float(RAND_MAX));
+    // Explicitly compute in float to avoid missing half arithmetic operators on some platforms.
+    return half(float(x) + (float(y) - float(x)) * float(std::rand()) / float(RAND_MAX));
   }
   static inline half run()
   {
