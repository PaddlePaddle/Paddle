- api : atan2
  inputs :
    x : X1
    y : X2
  outputs :
    out : Out

- api : cross
  inputs : {x : X, y : Y}
  attrs :
    axis : dim
  outputs :
    out : Out

- api : diagonal
  inputs :
    x : Input
  outputs :
    out : Out

- api : trace
  inputs :
    x : Input
  outputs :
    out : Out

- api : conv2d
  extra :
    attrs : {use_cudnn : (bool, false), fuse_relu_before_depthwise_conv : (bool, false), use_mkldnn : (bool, false), use_quantizer : (bool, false), mkldnn_data_type : (str, "float32"), fuse_relu : (bool, false), fuse_activation : (bool, ""), fuse_alpha : (bool, false), fuse_beta : (bool, false), use_addto : (bool, false), fuse_residual_connection : (bool, false), Scale_in : (float, 1.0f), Scale_out : (float, 1.0f), Scale_in_eltwise : (float, 1.0f), Scale_weights : (float[], {1.0f}), force_fp32_output : (bool, false), workspace_size_MB : (int, 512), exhaustive_search : (bool, false)}
