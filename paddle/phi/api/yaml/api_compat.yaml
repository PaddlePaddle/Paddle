- api : atan2
  inputs :
    x : X1
    y : X2
  outputs :
    out : Out

- api : cross
  inputs : {x : X, y : Y}
  attrs :
    axis : dim
  outputs :
    out : Out

- api : diagonal
  inputs :
    x : Input
  outputs :
    out : Out

- api : trace
  inputs :
    x : Input
  outputs :
    out : Out
