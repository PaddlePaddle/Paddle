name: Approval

on:
  pull_request:
    types: [opened, synchronize]

env:
  BRANCH: ${{ github.base_ref }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_ID: ${{ github.event.pull_request.number }}
  COMMIT_ID: ${{ github.event.pull_request.head.sha }}
  token: ${{ vars.ACTION_GITHUB_TOKEN }}

jobs:
  check-bypass:
    name: Check bypass
    uses: ./.github/workflows/check-bypass.yml
    with:
      workflow-name: 'approval'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  check-skill:
    name: Check file paths
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
      actions: read
    outputs:
      can-skip: ${{ steps.skip-skill.outputs.can-skip || 'false' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run skip check
        id: skip-skill
        uses: ./.github/actions/skip-skill
        with:
          ignore-paths: 'skill/**,.github/**'

  check-approvers:
    name: Check approval
    needs: [check-bypass, check-skill]
    if: ${{ needs.check-bypass.outputs.can-skip != 'true' && needs.check-skill.outputs.can-skip != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 200

      - name: Update paddle
        run: |
          git switch -c test
          git remote add upstream https://github.com/PaddlePaddle/Paddle.git
          git fetch upstream $BRANCH
          git checkout $BRANCH
          git config --global user.name "PaddleCI"
          git config --global user.email "paddle_ci@example.com"
          git merge test

      - name: Display Required Approvers
        run: |
          bash ci/check_approval.sh
