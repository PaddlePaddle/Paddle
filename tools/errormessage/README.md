usage method:
Please run:
```
bash start.sh < version > (optional) < URL > (optional)
```

If there is no parameter, the error message of cuda9.0/cuda10.0/cuda-latest-version will be crawled by default.

When you need to crawl a new web page, the format is: 
```
bash start.sh 91,92 url ur2
```
